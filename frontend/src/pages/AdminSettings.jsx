export default function AdminSettings() {
  return <div className="screen" style={{ background: 'var(--dark)', justifyContent: 'center', alignItems: 'center' }}>
    <div style={{ color: 'var(--white)' }}>Configuracion — proximamente</div>
  </div>
}