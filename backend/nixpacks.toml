[phases.setup]
nixPkgs = ["libGL", "libGLU", "glib"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip uninstall -y opencv-python opencv-contrib-python || true",
    "pip install opencv-python-headless",
    "pip install -r requirements.txt"
]